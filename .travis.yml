# This file was autogenerated and will overwrite each time you run travis_pypi_setup.py
sudo: required
dist: trusty
language: python
python: 2.7
services:
  - docker
virtualenv:
  system_site_packages: true
install:
  - pip install -U tox
  - pip install coverage==4.0
script:
  - tox
  - docker build -t urlshortener .
  - docker run -di -p 5000:5000 -v /tmp/var:/app/var urlshortener
  - ls /tmp/var
